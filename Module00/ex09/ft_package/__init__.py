from .count_in_list import count_in_list

__all__ = ["count_in_list"]

# when python sees a folder with an __init__.py in it,
# it treates that folder as a package.
# __init__.py runs automatically the moment someone imports the package.
